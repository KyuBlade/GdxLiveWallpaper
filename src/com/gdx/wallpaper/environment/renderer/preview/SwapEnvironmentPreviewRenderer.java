package com.gdx.wallpaper.environment.renderer.preview;

import com.gdx.wallpaper.environment.type.SwapEnvironment;

public class SwapEnvironmentPreviewRenderer extends EnvironmentPreviewRenderer<SwapEnvironment> {

    public SwapEnvironmentPreviewRenderer(SwapEnvironment environment) {
        super(environment);
    }
}