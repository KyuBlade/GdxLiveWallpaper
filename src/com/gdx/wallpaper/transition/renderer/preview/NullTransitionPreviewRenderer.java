package com.gdx.wallpaper.transition.renderer.preview;

import com.gdx.wallpaper.transition.type.NullTransition;

public class NullTransitionPreviewRenderer extends TransitionPreviewRenderer<NullTransition> {

    public NullTransitionPreviewRenderer(NullTransition transition) {
        super(transition);
    }
}